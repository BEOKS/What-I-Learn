spring:
  sql:
    init:
      mode: always
junit:
  jupiter:
    execution:
      parallel:
        enabled: true
        mode:
          default: concurrent
          classes:
            defalut: concurrent